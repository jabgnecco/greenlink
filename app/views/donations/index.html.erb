<h1>All donations:</h1>
<div>
  <% @donations.each do | donation | %>
    <div>
      <p class="fs-5 fw-bold"><%= donation.project.title %></p>
      <%= donation.amount %>
    </div>
  <% end%>
</div>
